# Required for React Native — Gradle and CocoaPods cannot resolve
# pnpm's default symlinked node_modules structure.
# Hoisted layout ensures native build scripts can find transitive deps
# (e.g. react-native-worklets required by react-native-reanimated).
node-linker=hoisted
